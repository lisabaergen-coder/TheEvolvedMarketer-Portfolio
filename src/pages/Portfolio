import React from 'react';
import { ExternalLink, Clock } from 'lucide-react';

const Portfolio = () => {
  const companies = [
    { name: "NuData Security" },
    { name: "Mastercard" },
    { name: "LexisNexis" },
    { name: "BehavioSec" },
    { name: "Lynx Tech" }
  ];

  return (
    <div className="min-h-screen bg-white">
      <section className="py-20 bg-gray-50">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h1 className="text-5xl md:text-6xl font-bold mb-8 font-montserrat" style={{ color: '#004E53' }}>
            Portfolio
          </h1>
          <p className="text-2xl text-gray-700 max-w-3xl mx-auto leading-relaxed font-helvetica">
            A track record of driving growth across industry-leading companies
          </p>
        </div>
      </section>

      <section className="py-16">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="bg-gradient-to-r from-teal-50 to-blue-50 rounded-2xl p-12 text-center border border-teal-200">
            <div className="flex justify-center mb-6">
              <Clock className="h-16 w-16 text-teal-600" />
            </div>
            <h2 className="text-3xl md:text-4xl font-bold mb-6 font-montserrat" style={{ color: '#004E53' }}>
              Case Studies Coming Soon
            </h2>
            <p className="text-xl text-gray-700 mb-8 max-w-2xl mx-auto font-helvetica">
              I'm currently compiling detailed case studies showcasing the strategies and results from my work with leading companies. Check back soon for in-depth insights.
            </p>
            <a
              href="https://calendly.com/theevolvedmarketer"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center px-8 py-4 bg-teal-700 text-white font-semibold rounded-lg hover:bg-teal-800 transition-colors duration-200 font-helvetica"
            >
              <ExternalLink className="h-5 w-5 mr-3" />
              Schedule a Call to Learn More
            </a>
          </div>
        </div>
      </section>

      <section className="py-20 bg-white">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-4xl md:text-5xl font-bold mb-8 font-montserrat" style={{ color: '#004E53' }}>
              Trusted by Industry Leaders
            </h2>
            <p className="text-xl text-gray-700 max-w-3xl mx-auto font-helvetica">
              Companies I've had the privilege to work with and help scale
            </p>
          </div>

          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8 items-center">
            {companies.map((company, index) => (
              <div 
                key={index} 
                className="bg-gray-50 rounded-xl p-8 flex items-center justify-center hover:shadow-lg transition-shadow duration-300"
              >
                <div className="text-center">
                  <div className="w-24 h-16 bg-gray-300 rounded-lg mb-3 flex items-center justify-center">
                    <span className="text-sm font-semibold text-gray-600 font-helvetica">
                      LOGO
                    </span>
                  </div>
                  <p className="text-sm font-medium text-gray-700 font-helvetica">
                    {company.name}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="py-20 bg-gray-50">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-4xl font-bold mb-8 font-montserrat" style={{ color: '#004E53' }}>
              What to Expect in the Case Studies
            </h2>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              {
                title: "Strategic Frameworks",
                description: "Detailed breakdown of go-to-market strategies, positioning, and messaging frameworks used."
              },
              {
                title: "Growth Metrics",
                description: "Quantifiable results including revenue growth, pipeline expansion, and ROI measurements."
              },
              {
                title: "Implementation Insights",
                description: "Behind-the-scenes look at team building, process optimization, and operational excellence."
              }
            ].map((item, index) => (
              <div key={index} className="bg-white rounded-2xl p-8 shadow-lg text-center">
                <h3 className="text-2xl font-bold mb-4 font-montserrat" style={{ color: '#004E53' }}>
                  {item.title}
                </h3>
                <p className="text-gray-700 leading-relaxed font-helvetica">
                  {item.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="py-20 bg-white">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h2 className="text-4xl md:text-5xl font-bold mb-8 font-montserrat" style={{ color: '#004E53' }}>
            Want to Be My Next Success Story?
          </h2>
          <p className="text-xl text-gray-700 mb-12 max-w-2xl mx-auto font-helvetica">
            Let's discuss how my proven methodologies can drive similar results for your company.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="https://calendly.com/theevolvedmarketer"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center px-8 py-4 bg-teal-700 text-white font-semibold rounded-lg hover:bg-teal-800 transition-colors duration-200 font-helvetica"
            >
              Schedule Strategy Call
            </a>
            <a
              href="mailto:lisa@theevolvedmarketer.com"
              className="inline-flex items-center px-8 py-4 bg-white border-2 border-teal-700 text-teal-700 font-semibold rounded-lg hover:bg-teal-50 transition-colors duration-200 font-helvetica"
            >
              Send Email
            </a>
          </div>
        </div>
      </section>
    </div>
  );
};

export default Portfolio;
